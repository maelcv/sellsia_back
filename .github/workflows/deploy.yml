name: CI/CD Backend Express

on:
  push:
    branches: [ "cicd" ] # Se déclenche au push sur la branche cicd

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v3

      - name: ⚙️ Configuration de Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22' # Mets la version que tu utilises

      - name: 🏗️ Installation et vérification (Build)
        run: |
          npm ci
          # npm run build # Décommente cette ligne si tu utilises TypeScript

      - name: 🧹 Nettoyer l'ancienne version sur le serveur
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/ceremia/api
            rm -rf src ia_models package*.json server.js prisma node_modules 2>/dev/null || true

      - name: 🚀 Déploiement des fichiers sur le serveur
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "src,ia_models,package.json,package-lock.json,server.js,prisma"
          target: "/var/www/ceremia/api"

      - name: 🔄 Redémarrage de l'API avec PM2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
<<<<<<< HEAD
            bash -ic 'cd /var/www/ceremia/api && npm install --production && npm install -g pm2 && npx prisma migrate deploy && pm2 delete api-express 2>/dev/null; pm2 start server.js --name "api-express" && pm2 save'
=======
            set -e
            cd /var/www/ceremia/api

            npm ci --omit=dev
            npm install -g pm2

            # Required by Prisma models using pgvector columns.
            printf 'CREATE EXTENSION IF NOT EXISTS vector;\n' | npx prisma db execute --stdin --schema prisma/schema.prisma

            MIGRATION_NAME="20260417000000_baseline"

            set +e
            MIGRATE_OUTPUT="$(npx prisma migrate deploy 2>&1)"
            MIGRATE_EXIT=$?
            set -e

            echo "$MIGRATE_OUTPUT"

            if [ "$MIGRATE_EXIT" -ne 0 ]; then
              if echo "$MIGRATE_OUTPUT" | grep -Eq 'P3009|P3018|already exists|SqlState\(E42710\)|SqlState\(E42P07\)'; then
                HAS_CORE_SCHEMA="$(node --input-type=module - <<'NODE'
            import { PrismaClient } from '@prisma/client';

            const prisma = new PrismaClient();
            try {
              const [row] = await prisma.$queryRawUnsafe(`
                SELECT
                  (to_regclass('public.users') IS NOT NULL) AS users_exists,
                  (to_regclass('public.workspaces') IS NOT NULL) AS workspaces_exists
              `);
              const hasUsers = row?.users_exists === true;
              const hasWorkspaces = row?.workspaces_exists === true;
              console.log(hasUsers && hasWorkspaces ? '1' : '0');
            } catch {
              console.log('0');
            } finally {
              await prisma.$disconnect();
            }
            NODE
                )"

                if [ "$HAS_CORE_SCHEMA" = "1" ]; then
                  echo "Migration baseline deja appliquee dans une base existante, resolution Prisma..."
                  npx prisma migrate resolve --applied "$MIGRATION_NAME" || true
                  npx prisma migrate deploy
                else
                  echo "Echec migration sans schema existant detecte. Arret pour securite."
                  exit "$MIGRATE_EXIT"
                fi
              else
                exit "$MIGRATE_EXIT"
              fi
            fi

            if pm2 describe api-express >/dev/null 2>&1; then
              pm2 restart api-express --update-env
            else
              pm2 start server.js --name "api-express"
            fi

            pm2 save
>>>>>>> dev
            